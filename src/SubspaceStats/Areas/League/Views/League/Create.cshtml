@using SubspaceStats.Areas.League.Models.League
@model LeagueViewModel

@{
    ViewData["Title"] = "Create League";

    IEnumerable<SelectListItem> gameTypes =
        from kvp in Model.GameTypes
        select new SelectListItem(kvp.Value, kvp.Key.ToString());
}

<h1>Create league</h1>

<div class="mb-3">
    <a asp-action="Index">Back to League List</a>
</div>

<div class="container">
    <form asp-controller="League" asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="mb-3">
            <label asp-for="League.Name" class="form-label"></label>
            <input asp-for="League.Name" class="form-control" />
            <span asp-validation-for="League.Name" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="League.GameType" class="form-label"></label>
            <select asp-for="League.GameType" asp-items="@gameTypes" class="form-select">
                <option disabled selected>-- Select a Game Type --</option>
            </select>
            <span asp-validation-for="League.GameType" class="text-danger"></span>
        </div>
        <div class="d-flex">
            <button type="submit" class="btn btn-primary m-2">Create</button>
            <a asp-action="Index" class="btn btn-secondary m-2">Cancel</a>
        </div>
        
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}