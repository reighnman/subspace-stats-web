@using SubspaceStats.Areas.League.Models.Franchise
@model FranchiseModel

@{
    ViewData["Title"] = "New Franchise";
}

<partial name="_FranchiseNav" />

<h1>New Franchise</h1>

<div class="mb-3">
    <a asp-action="Index" class="mx-2">Back to Franchise List</a>
</div>

<div class="container">
    <form asp-controller="Franchise" asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="mb-3">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="d-flex">
            <button type="submit" class="btn btn-primary m-2">Create</button>
            <a asp-action="Index" class="btn btn-secondary m-2">Cancel</a>
        </div>

    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}